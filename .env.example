# URL de l'API backend (Express) consommée par l'interface admin (/admin).
# En développement local, le backend tourne généralement sur http://localhost:3001/api.
NEXT_PUBLIC_API_URL=http://localhost:3001/api
